<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.zerock.mapper.WonhyeokMapper">

<select id="getsearchbystorename" resultType="org.zerock.domain.AddressVO">
SELECT 
	storename storename, 
	storeaddress storeaddress,
	storePhonenum storePhonenum, 
	storelat storelat, 
	storelag storelag

FROM storeinfo

WHERE storename like CONCAT('%', #{storename}, '%')
</select>

<select id="getSearchArroundPlaces" resultType="org.zerock.domain.AddressVO">
<![CDATA[

SELECT
	storename storename,
	storeaddress storeaddress,
	storePhonenum storePhonenum, 
	storelat storelat,
	storelag storelag
FROM storeinfo

WHERE
	  storelat > #{storelat} - 0.005 AND storelat < #{storelat} + 0.005 AND
	  storelag > #{storelag} - 0.005 AND storelag < #{storelag} + 0.005

 ]]>
</select>


</mapper>